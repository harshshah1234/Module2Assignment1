const greetTheStudent = studentName => `Hello there ${studentName}`;

const fullName = (firstName, middleName, lastName) => `${lastName}, ${middleName}, ${firstName}`;
const titleName = fullName('Hrishee', 'K', 'Shah');
console.log(titleName);

